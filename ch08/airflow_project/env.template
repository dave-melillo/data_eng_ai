# Airflow Web UI Credentials (leave as default)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# API Keys (get from respective websites)
# NewsAPI: https://newsapi.org/
# OpenAI: https://platform.openai.com/api-keys
NEWS_API_KEY=your_news_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# PostgreSQL Database Connection
# This is YOUR PostgreSQL database where news articles will be stored
# NOT the Airflow metadata database (which uses SQLite)

# PGHOST options:
# - For local PostgreSQL: try 'localhost' first
# - If localhost doesn't work: use 'host.docker.internal'
# - For cloud PostgreSQL: use your cloud provider's hostname
PGHOST=localhost

PGPORT=5432
PGDATABASE=news_db
PGUSER=news_user
PGPASSWORD=your_db_password_here

# Docker user (leave as default)
AIRFLOW_UID=50000
